Empty on purpose.

This folder held photos from the forked template and they have been deleted.
The Gallery feature itself is kept: src/components/Gallery.tsx still exists,
it is just not rendered right now.

NEW GALLERY PHOTOS DO NOT GO HERE. Paste them into:

    src/assets/photos/gallery/

Then ask for the Gallery section to be switched back on (one line in
src/App.tsx and one nav entry in src/components/Nav.tsx).
